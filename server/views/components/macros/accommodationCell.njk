{%- macro accommodationCell(context) -%}
  <span class="sas-table__cell-header" aria-hidden="true">{{ 'Current accommodation' if context.cellType === 'current' else 'Next accommodation' }}</span>

  {{ context.accommodationType }}

  {%- if context.isPrivate -%}
    {%- if context.addressLine1 %}
      <br />
      <span class="govuk-hint">{{ context.addressLine1 }}</span>
    {% endif -%}
  {%- else -%}
    {%- if context.cellType !== 'current' and context.startDate and context.status !== 'SETTLED' and context.status !== 'TRANSIENT' %}
      <br />
      <span class="govuk-hint">From {{ context.startDate | date }}</span>
    {% endif -%}

    {%- if context.endDate %}
      <br />
      <span class="govuk-hint">Until {{ context.endDate | date }}</span>
    {% endif -%}
  {%- endif -%}
{%- endmacro -%}
