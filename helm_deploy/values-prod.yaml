---
# Per environment values which override defaults in hmpps-single-accommodation-service-ui/values.yaml

generic-service:
  ingress:
    host: single-accommodation-service-ui.hmpps.service.justice.gov.uk

  env:
    INGRESS_URL: "https://single-accommodation-service-ui.hmpps.service.justice.gov.uk"
    HMPPS_AUTH_URL: "https://sign-in.hmpps.service.justice.gov.uk/auth"
    TOKEN_VERIFICATION_API_URL: "https://token-verification-api.prison.service.justice.gov.uk"
    EXAMPLE_API_URL: 'https://template-kotlin.hmpps.service.justice.gov.uk'
    AUDIT_ENABLED: "false"

generic-prometheus-alerts:
  alertSeverity: hmpps-approved-premises
